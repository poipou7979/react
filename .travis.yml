sudo: required
services:
  - docker

before_install:
  - docker build -t pourya/front-react -f Dockerfile.dev .
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.10.1
  - export PATH=$HOME/.yarn/bin:$PATH
script:
  - docker run pourya/front-react npm run test -- --coverage
